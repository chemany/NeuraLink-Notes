# Node.js dependencies
node_modules/
/frontend/node_modules/
/backend/node_modules/

# Lockfiles - Usually you WANT to commit these for applications to ensure reproducible builds
# Decide if you want to commit package-lock.json. If so, remove the line below or comment it.
# For now, I will assume you want to commit it as it's good practice for apps.
# package-lock.json

# Build artifacts
/frontend/.next/
/frontend/out/
/backend/dist/

# Environment variables
.env
.env*.local
*.env

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS generated files
.DS_Store
Thumbs.db

# IDE specific
.vscode/
.idea/
.cursor/

# Compressed files (based on your git status output)
# You can uncomment the specific lines if you want to ignore these .7z or .json files
# *.7z
# "notebook-lm-clone可能不需要文件.7z" # Exact name if needed, careful with encoding
# "自动发布工具配置.json"
# "飞书多维表格记录.json"

# Temporary files from specific tools if any
# e.g., frontend/pdfjs.zip - if this is a temporary download, consider ignoring
# frontend/pdfjs.zip

# Prisma (backend) - generated client is in node_modules, but if you have other generated files
# backend/prisma/generated/

# Test coverage
coverage/

# If you want to ignore your specific .7z and .json files, 
# add their names here, for example:
# notebook-lm-clone可能不需要文件.7z
# 自动发布工具配置.json
# 飞书多维表格记录.json
# frontend/pdfjs.zip
